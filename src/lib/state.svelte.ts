export const typing = $state({ current: false });
export const ignoreScroll = $state({ current: false });
export const focused = $state({ current: false });
export const value: { current: string | null } = $state({ current: null });
